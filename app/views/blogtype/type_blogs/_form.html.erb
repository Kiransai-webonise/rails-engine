<%= form_with(model: type_blog) do |form| %>
  <% if type_blog.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(type_blog.errors.count, "error") %> prohibited this type_blog from being saved:</h2>

      <ul>
        <% type_blog.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :blog_type, style: "display: block" %>
    <%= form.text_field :blog_type %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_area :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
